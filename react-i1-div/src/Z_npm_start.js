---------------- rsc ------------------

Creates a stateless React component without PropTypes and ES6 module system(Reactjs code snippets)

import React from 'react';

const = () => {
return (
<div>

</div>
);
};

export default ;


------------------
rsp

Creates a stateless React component with PropTypes and ES6 module system(Reactjs code snippets)

import React from 'react';
import PropTypes from 'prop-types';

const = props => {
return (
<div>

</div>
);
};

.propTypes = {

};

export default ;
